# 2026-03-31 日记

- Session bootstrap created this file because it was missing after crossing midnight.
- 对 `2026-03-30` 批量上移形成的 `110` 家薄 `L3` 做了一轮公开披露补强。
- 同步更新了：
  - `静态潜客主表.xlsx`
  - `潜客档案库.xlsx`
  - `治理与证据.xlsx`
  - `L3以上客户档案索引-团队共享.xlsx`
  - `07-L3以上客户档案/02-L3档案/` 下对应 `110` 个档案页
- 本轮将这 `110` 家的：
  - `信息扎实度` 统一提升到 `中高`
  - `source_note` 改为 `公开主体名单批量扩展 2026-03-30；2026-03-31 上市公司公开披露补强`
  - `primary_source_types` 改为 `公开主体名单,行业分类,上市公司公开披露`
- 修正了一处重要口径：
  - 不再用市值替代 `收入规模区间`
  - 这 `110` 家统一改为 `待补公开财报口径`
- 新增专项记录：
  - `docs/03-执行与校验/静态潜客池-薄L3补全专项-v1.md`
- 对当前全部 `167` 家 `L3` 又做了一轮统一补强与纠错。
- 本轮结果：
  - `165` 家成功补到 `公开主体名单,行业分类,上市公司公开披露`
  - `信息扎实度` 提升到 `中高`
  - 统一补了产品/商业模式/客户客群/当前业务问题/JTBD/数字化项目动态/近一年重大事件
  - `收入规模区间` 全部统一成 `待补公开财报口径`
- 仅剩 `2` 家未稳定匹配公开披露入口：
  - `追觅创新科技（苏州）有限公司`
  - `深圳市通拓科技有限公司`
- 这两家本轮只完成了纠错与待补项收束，不硬抬。
- 本轮同步重生成了全部 `167` 个 `L3` 客户档案页，并更新了：
  - `静态潜客主表.xlsx`
  - `潜客档案库.xlsx`
  - `治理与证据.xlsx`
  - `L3以上客户档案索引-团队共享.xlsx`
- 新增专项记录：
  - `docs/03-执行与校验/静态潜客池-L3全量补强专项-v1.md`
- 在不降低标准的前提下，又完成一轮 `L3 -> L2` 批量上移。
- 使用门槛：
  - `信息扎实度=中高`
  - `ICP匹配概率=中高/高`
  - `profile_status=standard_ready`
  - `official_source_count>=1`
  - `high_confidence_source_count>=2`
  - `primary_source_types=公开主体名单,行业分类,上市公司公开披露`
- 本轮结果：
  - `167` 个 `L3` 中，`118` 个上移到 `L2`
  - 剩余 `49` 个继续留在 `L3`
  - 主表分层更新为 `L1=8 / L2=258 / L3=49 / L4=1 / L5=259`
- 同步更新了：
  - `静态潜客主表.xlsx`
  - `潜客档案库.xlsx`
  - `治理与证据.xlsx`
  - `L3以上客户档案索引-团队共享.xlsx`
  - `L3以上客户档案索引.md`
  - `潜客池-首页.md`
  - `内部总览-静态潜客池-总池汇总.md`
  - `内部状态-静态潜客池-Milestone状态总览.md`
- 新增专项记录：
  - `docs/03-执行与校验/静态潜客池-L3到L2批量上移专项-v3.md`
- 继续处理剩余 `49` 家 `L3`，按“不降低标准”原则再次收口：
  - `47` 家在完成 `ICP` 校准后上移到 `L2`
  - 仅剩 `2` 家继续保留在 `L3`：
    - `追觅创新科技（苏州）有限公司`
    - `深圳市通拓科技有限公司`
- 当前最新分层：
  - `L1=8 / L2=305 / L3=2 / L4=1 / L5=259`
  - `L3+ = 315`
- 同步动作：
  - `47` 个客户档案页移入 `01-L1-L2档案`
  - `L3以上客户档案索引-团队共享.xlsx` 更新为 `L1=8 / L2=305 / L3=2`
  - 清理了 `2` 个历史残留的错误 `L3` 档案页（`千味央厨 / 纽威阀门`），并移到回收站
- 新增专项记录：
  - `docs/03-执行与校验/静态潜客池-L3到L2批量上移专项-v4.md`
- 在不降低标准的前提下，又完成一轮 `L5 -> L3` 批量扩容：
  - 仅处理 `110` 家 `公开主体名单批量扩展 2026-03-30` 的 `L5`
  - 前提是：已过老客排除、主体去重和稳定公开披露入口判断
- 本轮统一补充了：
  - 产品与服务、商业模式、客户客群
  - 当前业务问题、JTBD
  - 已上线系统概况、数字化项目动态、近一年重大事件
  - `official_source_count = 1`
  - `high_confidence_source_count = 2`
  - `profile_status = standard_ready`
- 本轮结果：
  - `110` 家全部完成 `L5 -> L3`
  - 当前分层更新为 `L1=8 / L2=305 / L3=112 / L4=1 / L5=149`
  - `L3+ = 425`
- 同步更新了：
  - `静态潜客主表.xlsx`
  - `潜客档案库.xlsx`
  - `治理与证据.xlsx`
  - `L3以上客户档案索引-团队共享.xlsx`
  - 新增 `110` 个 `L3` 客户档案页
  - 重生成 `L3以上客户档案索引.md`
- 新增专项记录：
  - `docs/03-执行与校验/静态潜客池-L5到L3扩容专项-v2.md`
- 新增 `L3 -> L2` 候选批次 `v5`：从当前 `112` 家 `L3` 中筛出 `30` 家下一轮优先上移候选（零售消费 14、跨境电商 6、先进制造 10），并同步挂入 `治理与证据.xlsx / review_queue`。
- 整理了当前团队可分享内容清单：默认主包为 `L3+ 客户档案索引 Excel + L3+ 客户档案页`，可选补充为 `3` 个专题包与按需重点公司 note；不再默认分享内部运营工作簿、全量浏览视图、evidence 与 archive。
- 生成首次团队分享包候选：`/Users/clairaipartner/Documents/Obsidian-Codex/潜客池/首次团队分享包候选.xlsx`，推荐首次主包为 `L1/L2 共 313 家`，`L3 共 112 家` 先作为候补层；并新增说明页 `首次团队分享包候选说明.md`。
- 完成首次团队分享 admin 确认：`L1/L2 = 313` 标记为 `已分享`（批次 `share_batch_2026_03_31_v1`），`L3 = 112` 保留为未分享候补；生成 `首次团队分享包候选.xlsx` 与 `首次团队分享-admin确认记录-v1.md`，并同步修正首页、档案索引和档案说明页。
- 停用了无效话术资产 `ka_narrative_group_dashboard_mfg_v1`：已从 `知识资产注册表.xlsx` 删除、从 Obsidian 引用中批量清理，并将原 note 移入系统回收站；相关页面改为保留有效制造话术或显示“待补制造话术资产”。
- 对话术/叙事资产做了有效性清理：停用并移除 `9` 条仅依赖 `internal://narrative` 或自我文档来源的无效资产（含 `ka_narrative_medtech_mfg_v1`），同步清理注册表、`潜客档案库.xlsx/talk_track_refs` 与 Obsidian 阅读层引用；当前保留的有效叙事类资产仅限挂接真实原始素材的条目。
- 基于真实原始素材重建了第一批 `5` 条有效话术资产：
  - `ka_talktrack_hq_visibility_retail_v1`
  - `ka_talktrack_frontline_loop_retail_v1`
  - `ka_talktrack_inventory_replenishment_retail_v1`
  - `ka_talktrack_profit_governance_cbec_v1`
  - `ka_talktrack_group_coordination_mfg_v1`
- 同步更新：
  - `知识资产注册表.xlsx` 新增 `5` 条 `sales_narrative`，正式知识资产总数从 `95` 提升到 `100`
  - `潜客档案库.xlsx` 中 `55` 条旧占位 `talktrack_*` 引用切换到新资产 id
  - `59` 个 Obsidian markdown 页面中的旧占位 id 同步切换
  - `经营诉求画像` 相关说明页与 `3` 个专题包已开始挂回新的有效话术资产
- 当前有效话术资产重建原则进一步固定：
  - 只允许基于真实原始素材建立
  - 不允许基于 `internal://narrative/...` 或内部占位文档直接进入正式知识资产层
- 将 `3` 个偏消费品的细分画像从 `draft` 升级为正式 `active`：
  - `retail_brand_beauty`
  - `retail_brand_maternal_pet`
  - `retail_fashion_group`
- 同步完成：
  - `02-画像/` 下新增 `3` 个画像页
  - `主线与画像注册表.xlsx` 中 `personas` 增加/更新对应 `active` 记录
  - `persona_research_tags` 中这 `3` 条改为 `promoted_to_active`
  - `零售消费.md` 与 `retail_high_sku_brand.md` 已改到新口径
  - 当前业务形态画像总数更新为 `13`

- 完成消费品新画像扩池：基于东方财富行业板块成分股新增 `220` 家 `L5`，当前主表口径更新为 `L1=8 / L2=305 / L3=112 / L4=1 / L5=369`；新增对象已同步写入主表、档案库、review_queue 和 evidence_log。

- 在不降低 `L5 -> L3` 最低门槛的前提下，完成消费品新画像批次 `220` 家 `L5 -> L3` 上移；同步更新主表、档案库、evidence、queue、团队索引和全部新 `L3` 档案页。

- 对全部 `L3+`（`645` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 继续滚动补完消费品新画像批次 `L3` 的 cninfo 官方源补强，现已累计完成 `220/220` 家，当前该批次剩余未补官方源对象为 `0`，后续重点转向官网、年报、IR 和财报口径字段。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `15` 家。

- 对全部 `L3+`（`645` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `12` 家。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `12` 家。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `14` 家。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `14` 家。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `14` 家。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `13` 家。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `14` 家。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `11` 家。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `13` 家。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `14` 家。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `15` 家。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `15` 家。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `15` 家。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `15` 家。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `15` 家。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `15` 家。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `15` 家。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `15` 家。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `15` 家。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `14` 家。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `15` 家。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `15` 家。

- 对消费品新画像批次中已上移到 `L3` 的 A 股主体做了一轮 cninfo 官方源补强，成功补强 `13` 家。

- 对全部 `L3+`（`645` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`645` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对 `L3+` 客户档案名称做了一轮公司全称标准化，成功校正 `217` 个简称主体；仍有 `4` 个对象暂未能自动反查全称。

- 对 `L3+` 客户档案名称做了一轮公司全称标准化，成功校正 `3` 个简称主体；仍有 `1` 个对象暂未能自动反查全称。

- 对 `L3+` 客户档案名称做了一轮公司全称标准化，成功校正 `0` 个简称主体；仍有 `1` 个对象暂未能自动反查全称。

- 对 `L3+` 客户档案名称做了一轮公司全称标准化，成功校正 `0` 个简称主体；仍有 `1` 个对象暂未能自动反查全称。

- 完成一轮 `L3 -> L2` 批量上移，实际新增上移 `330` 家；其中 `220` 家基于消费品新画像与官方源补强重判后进入 `L2`。

- 对全部 `L3+`（`645` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`645` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`645` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`645` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`645` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`645` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`645` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`645` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`645` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`645` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`645` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`645` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`645` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`645` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`645` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`651` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`640` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。

- 对全部 `L3+`（`634` 家）做了一轮信息补强与纠错：统一纠正财报口径空值、补齐竞品/招聘/相似样本默认口径，并重生成全部客户档案页与团队共享索引。
