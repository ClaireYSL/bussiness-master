# 2026-04-04 日记

- Session bootstrap created this file because it was missing.
- 继续客户档案逐客户修复工作，确认 `batch_37` 后真实累计状态为 `已修复=395 / 待复核=31 / 未修复=206`。
- 单批推进完成 `batch_38` 到 `batch_52`，累计推进到 `已修复=565 / 待复核=41 / 未修复=26`。
- `batch_53` 整批 `12/12` 进入待复核，暴露尾部样本共性问题：商业模式公司级事实明显不足。
- 收紧 `generate_customer_archive_repair_batch_20260403.py` 候选规则，要求商业模式必须先有可用公司级事实。
- `batch_54` 在新门槛下成为空批次，说明通用半自动修复链路已到尾部边界。
- 新增尾部修复分组清单：`docs/03-执行与校验/客户档案尾部修复分组清单-20260403.md`，把剩余 `未修复` 和 `待复核` 按产品事实不足、商业模式不足、整体重建三类收口，后续应转为定向修复。
- 本次回看仓库主线后确认：`客户档案逐客户修复` 只是 `Milestone 3` 的支持性工作，不是当前系统核心闭环；核心主线仍应是“知识库持续学习 -> 画像/主线建模 -> 基于画像扩展 L5 候选 -> 在上移过程中补强信息并重判”。
- 新增总览文档 `docs/00-当前总览/静态潜客池-主线恢复版总说明-v1.md`，把系统主目标、正确闭环、四层职责、默认优先级与“客户档案修复的正确位置”重新收束为单页入口。
- 已同步改写 `docs/00-当前总览/静态潜客池整体设计与运行机制汇报-v1.md` 与 `docs/00-当前总览/静态潜客池-Milestone状态总览-v1.0.md`，统一入口层口径为“知识库 -> 画像 -> 候选 -> 上移补强”，并将客户档案修复降回支持层。
- 已补写 `docs/01-机制与规则/客户档案体系重构与逐客户修复机制-v1.md` 的适用边界，明确该文档只约束档案层修复，不再被误读为整个项目的默认主流程。
- 新增 `docs/00-当前总览/静态潜客池-污染止血与可信底座重建方案-v1.md`，正式把项目阶段从“修档案”切到“止血 -> 可信最小底座 -> 小样本校准 -> 分层重建”。
- 新增执行规则 `docs/01-机制与规则/静态潜客池-冻结口径与可信最小字段集规范-v1.md`，明确旧档案冻结范围、可信最小字段集、可信来源顺序与小样本校准执行标准。
- 新增外部 LLM 最小接入脚本 `scripts/llm_delegate.py`，并补充 `docs/01-机制与规则/外部LLM委派边界与最小接入说明-v1.md` 与 `.env.example` 环境变量占位，约束只允许外发抽象后的知识与结构化输入，不允许外发原始知识源文件。
- 已新增外部 LLM 校准任务包模板：`prompts/delegate/calibration_system.md`、`prompts/delegate/calibration_user.md`、`prompts/delegate/calibration_input_template.json`，并在委派说明中补充最小调用示例。
- 已实现“执行保障代码最小改造方案 v1”第一轮：新增 `shared/static_pool/` 共享校验层，统一 `placeholder / review_status / L5 分流 / 上移闸门 / 冻结来源` 规则。
- 已改造 `scripts/expand_l5_consumer_personas_20260331.py`：不再新写 `queued`，改为写前先跑共享校验，并按正式候选 / 观察对象分流 `review_status` 与 `queue_type`。
- 已改造 `scripts/promote_l5_to_l3_consumer_20260331.py`：读取时兼容 `queued -> pending_review`，接入共享上移闸门并输出 `allow / warn / block` 统计与样本摘要。
- 已改造 `scripts/repair_customer_archive_batch_20260403.py`：明确“阅读层修复不等于上移完成”，并将 `validation_gap` 收紧为具体缺口。
- 已改造 `scripts/llm_delegate.py`：新增 `--input-source-tag`，外部委派会拒绝冻结来源标签。
- 已新增 `scripts/build_calibration_batch.py`：可从主表 / 档案 / evidence 生成外部 LLM 校准任务包，并附带本地 L5 校验与上移闸门结果。
- 已补充基础验证：`python3 -m py_compile` 通过，`python3 -m unittest tests.static_pool.test_validators -v` 通过 `6` 项。
- 已回查旧 workspace，确认“知识库原素材位置”和“学习进展”并非缺失，而是主要沉淀在 `/Users/clairaipartner/Documents/Obsidian-Codex/潜客池/知识资产注册表.xlsx` 的 `raw_material_inventory / learning_queue / knowledge_assets` 三张 sheet 中，并补写总览文档 `docs/00-当前总览/知识库原素材位置与学习进展总览-v1.md`。
- 已补两份只读盘点文档，不启动新的学习动作：`docs/00-当前总览/原素材物理目录分布总览-v1.md` 与 `docs/00-当前总览/当前queued学习队列复核清单-v1.md`。当前确认原素材主要集中在旧 `geo-content` 与 `dingtalk_docs/2026-03-28`，`queued` 实际由 `10` 条真实待学习素材 + `1` 条手工入口占位组成。
- 已新增迁移建议文档 `docs/00-当前总览/知识原素材稳定根目录迁移建议-v1.md`，明确当前知识原素材虽然未丢失，但主要仍依赖旧 workspace 路径；建议未来优先把 `已转正式资产 / 已入学习队列 / queued / converted` 直接引用的真实素材迁入稳定新根目录，而不是继续寄居在历史工作区。
- 已新增 `docs/00-当前总览/知识原素材迁移执行清单-v1.md`，把第一优先级迁移范围收敛到 `65` 条唯一物理文件，其中 `geo_l1_cases=19`、`dingtalk_2026_03_28=46`；当前仍只做清单，不执行实际文件迁移。
- 已执行知识原素材全量复制整理，不删除旧文件：将 `知识资产注册表.xlsx / raw_material_inventory` 中登记的 `189` 条本地原素材全量复制到 `/Users/clairaipartner/Codex/static-pool-raw-materials`，按 `source_root / material_status / file_type` 分层；复制结果为 `copied=189 / missing=0`，并生成 `_manifest/copy_manifest.json` 与 `_manifest/README.md`。
- 已完成知识素材账本路径回写：`知识资产注册表.xlsx` 在写前已自动备份为 `知识资产注册表.backup_20260404_143504.xlsx`；随后将 `raw_material_inventory.material_path_or_url` 全量 `189` 条、`learning_queue.material_path_or_url` 共 `57` 条、`knowledge_assets.source_path_or_url` 共 `69` 条批量切换到 `/Users/clairaipartner/Codex/static-pool-raw-materials` 新根目录。
- 已确认当前文档治理应先于旧路径清理，并新增 `docs/00-当前总览/文档盘点与治理建议-v1.md`：当前 `docs/03-执行与校验` 共 `87` 份，其中文件 `客户档案修复批次-*` 就有 `54` 份；后续应优先做“现行入口 / 现行规则 / 现行结构 / 执行索引 / 历史归档”分层，再处理旧引用。
- 已新增 `docs/00-当前总览/03-执行与校验-归档迁移清单-v1.md`，明确 `03-执行与校验` 后续应优先迁移的对象：第一轮最确定的是 `54` 份 `客户档案修复批次-*`、`8` 份已被覆盖的多版本专项、`5` 份试运行/偏航记录；保留当前索引与少量仍复用专题在现行目录。
- 已执行 `03-执行与校验` 第一轮实际归档迁移：创建 `docs/archive/执行记录/{客户档案修复批次,专项过程,试运行记录}`，并实际迁移 `67` 份文件（`54+8+5`）；`docs/03-执行与校验` 现已从 `87` 份降至 `20` 份，同时补了 `docs/03-执行与校验/README.md` 并修正 `L5扩展批次校验索引-v1.md` 对试运行记录的链接。
- 已执行 `02-注册表与结构` 第一轮分层：创建 `docs/archive/结构演化/`，并迁移 `8` 份历史结构文档（首版真实内容、扩展内容、清洗后事实主表）；`docs/02-注册表与结构` 现已从 `16` 份降至 `8` 份，仅保留现行字段模板，并新增 `docs/02-注册表与结构/README.md` 作为结构入口说明。
- 已完成入口层收口：重写 `docs/README-静态潜客池.md` 作为 repo 权威导航页，明确当前主线、默认阅读顺序、事实源/知识源/阅读层位置及各目录职责；同时新增 `docs/00-当前总览/README.md`，把 `00-当前总览` 正式分成“核心入口”和“专题入口”。
- 已新增 `docs/01-机制与规则/README.md`，把规则层正式分成“核心机制 / 执行性规则 / 支撑专题”，并固定默认阅读顺序；至此 docs 的入口链已形成：总入口 -> 当前总览 -> 机制层 -> 结构层 -> 执行索引层。
- 已启动“校准样本集设计 v1”：新增 `docs/00-当前总览/校准样本集设计-v1.md`，确认第一轮采用 `三条主线 × 五层级 = 15 家` 全层覆盖样本；并生成 `deliveries/calibration_batch_phase1_v1.json` 及三条主线分包 `deliveries/calibration_batch_phase1_{retail,cbec,mfg}_v1.json`。
- 已修复 `scripts/build_calibration_batch.py` 在共享版主表回退场景下按 `account_id` 选样本失效的问题：现会先根据档案库回填 `account_id`，再做精确筛样。
- 已尝试调用外部 LLM 跑第一轮 `15` 家校准：先遇到 `response_format=json_object` 不被当前模型支持，已为 `scripts/llm_delegate.py` 增加自动回退；随后全量包请求触发 `429 Too Many Requests`，因此后续默认按三条主线分包委派。
- 已成功跑通第一包外部校准：`deliveries/calibration_batch_phase1_retail_v1.json -> deliveries/calibration_batch_phase1_retail_v1.delegate.json`。零售消费 `5` 家样本中，`4` 家主线/画像基本可保留，`1` 家（泡泡玛特）因缺少官方高可信来源被判 `minimum_fact=fail` 且建议降级；东鹏饮料被外部校准建议从 `L5` 可直接提升到 `L3`。
- 已完成三条主线 Phase 1 外部校准：跨境结果写入 `deliveries/calibration_batch_phase1_cbec_v1.delegate.json`，先进制造结果写入 `deliveries/calibration_batch_phase1_mfg_v1.delegate.json`；并新增横向复盘文档 `docs/00-当前总览/校准样本集-Phase1复盘-v1.md`。当前横向统计为：`track keep=12/15`、`persona adjust/uncertain=7/15`、`minimum_fact fail=3/15`、`maturity downgrade=5/15`，说明主线整体未塌，但画像边界、最小事实门槛和 `L4/L5` 口径需要马上收紧。
- 已将 Phase 1 校准结果转成规则文档 `docs/01-机制与规则/L4L5二次分流与画像收紧方案-v1.md`：明确 `L4/L5` 必须拆成“正式候选 / 观察对象”，并要求 persona 回到标准画像体系；第一批整改对象为 `acc_popmart / acc_loctek / acc_jereh`，第一批画像重判对象为 `acc_jihong / acc_zibuyu / acc_amec / acc_bozon`。
- 已新增权威关系文档 `docs/00-当前总览/知识库画像潜客池档案关系总说明-v1.md`，把 `知识库 -> 画像 -> 潜客 -> 潜客池 -> 潜客档案` 的单向主链、受控回流和强相关知识回挂规则一次性钉死；并同步更新 `docs/README-静态潜客池.md`、`docs/00-当前总览/README.md`、`静态潜客池-主线恢复版总说明-v1.md`、`静态潜客池整体设计与运行机制汇报-v1.md`、`docs/01-机制与规则/README.md`，统一指向这份主文档，减少相似文档继续互相影响。
- 已将上述关系口径正式下沉到结构层：更新 `docs/02-注册表与结构/external_target_account_pool_v2-字段模板-v1.md`、`docs/02-注册表与结构/潜客档案库-字段模板-v1.md`、`docs/02-注册表与结构/persona_registry_v1-字段模板-v1.md`，明确 `persona_tag` 为主画像、`secondary_persona_tags` 为次级画像、`knowledge_asset_refs` 为强相关知识资产引用。
- 已更新 `scripts/build_calibration_batch.py`，让校准任务包输出 `secondary_persona_tags`、`knowledge_asset_refs`、`talk_track_refs`，便于外部校准和后续人工复核直接消费“主画像 / 次级画像 / 强相关知识引用”。
- 已更新 `shared/static_pool/models.py` 的 `CandidateRecord`，补入 `secondary_persona_tags` 与 `knowledge_asset_refs`，保持共享模型和字段模板口径一致。
- 已启动 `Milestone 4：治理收口与 Phase 1 全样本纠偏` 的实际实现：在 `shared/static_pool/constants.py` 增加标准 persona 集合与 legacy persona alias map，并在 `shared/static_pool/validators.py` 中正式加入非标准 persona、legacy persona、次级画像不合规的校验闸门。
- 已更新 `shared/static_pool/reporting.py`、`shared/static_pool/__init__.py`、`scripts/expand_l5_consumer_personas_20260331.py`、`scripts/promote_l5_to_l3_consumer_20260331.py`，让主画像/次级画像/强相关知识引用口径真正进入核心脚本输出和阅读层渲染。
- 已新增 `scripts/build_phase1_rectification_package.py`，并生成 `deliveries/phase1_rectification_package_v1.json`，把 `Phase 1` 的 `15` 家样本正式收口为结构化纠偏结果：`formal_candidate=11 / observation=4 / downgrade=5 / maturity_regraded=9 / persona_adjusted=7 / official_source_repair_needed=4`。
- 已新增 `docs/00-当前总览/Phase1样本正式纠偏清单-v1.md`、`docs/00-当前总览/learning_queue与Phase1样本关联复核-v1.md`、`docs/00-当前总览/Milestone 4-治理收口与Phase1纠偏复盘-v1.md`，并同步更新 `docs/00-当前总览/README.md`、`docs/00-当前总览/静态潜客池-Milestone状态总览-v1.0.md`、`docs/01-机制与规则/L4L5二次分流与画像收紧方案-v1.md`。
- 已完成本轮 smoke check：`python3 -m py_compile` 覆盖核心改动文件通过；legacy persona 样本经 `classify_l5_candidate()` 会被程序识别为 `observation/pending_review`，证明 persona 收紧闸门已生效。
- 已新增 `docs/01-机制与规则/promote通用化改造方案-v1.md`，明确当前 `promote_l5_to_l3_consumer_20260331.py`、`promote_l3_to_l2_mass_20260331.py` 都属于历史专项脚本，不应继续演化成“每种上移一个脚本”的模式；后续正确方向是“一个通用 promote 引擎 + 批次配置 / 历史 wrapper”。
- 已完成 `promote` 通用化改造第一阶段落地：新增 `shared/static_pool/promote_engine.py` 抽取通用候选评估与批次结果输出能力，并新增 `scripts/promote_static_pool.py` 作为统一只读 promote 入口；已实际生成样例结果 `deliveries/promote_batch_retail_l5_to_l3_v1.json`，当前零售 `L5 -> L3` 小批评估结果为 `warn=2 / block=3`。
- 已继续推进 `promote` 通用化第二小步：`scripts/promote_static_pool.py` 现已支持 `--config-file` 批次配置文件；新增 `configs/promote_batches/retail_l5_to_l3_v1.json`、`configs/promote_batches/l3_to_l2_mass_v1.json` 和规则文档 `docs/01-机制与规则/promote批次配置模板-v1.md`。已验证配置文件模式可正常生成 `deliveries/promote_batch_retail_l5_to_l3_v1_from_config.json`，说明“通用入口 + 批次配置”链路已经打通。
- 已完成 `promote` 通用化第二阶段的最小 wrapper 化：`promote_l5_to_l3_consumer_20260331.py` 与 `promote_l3_to_l2_mass_20260331.py` 现都支持 `--report-only`，会先调用通用 preflight 引擎生成统一结果包；对应输出为 `deliveries/promote_batch_retail_l5_to_l3_v1_wrapper.json`（`warn=2 / block=3`）和 `deliveries/promote_batch_l3_to_l2_mass_v1_wrapper.json`（`warn=20`）。
- 已继续收薄历史 promote 脚本：默认主流程现在也会优先消费通用 preflight 选出来的 `account_id` 集合，不再各自重复做一轮完整候选发现；当前仍保留历史专项写回逻辑，但“候选发现/批次评估”已基本统一到通用入口。
- 已新增 `shared/static_pool/promotion_writeback.py`，将 `source_note` 追加、`validation_gap` 前缀补写、promotion evidence 去重补写、`promotion_review` 队列关闭等最小共享写回骨架统一抽出；`promote_l5_to_l3_consumer_20260331.py` 与 `promote_l3_to_l2_mass_20260331.py` 已改为消费这组 helper，编译通过，且两支历史 wrapper 的 `--report-only` smoke check 结果保持不变。
- 已继续下沉 `promote` 公共写回逻辑：`promotion_writeback.py` 现已覆盖主表核心升层写回、档案核心状态写回、coverage 核心状态写回；两支历史脚本现在主要只保留消费品专项文案、观察记录、档案渲染等专题差异。再次验证 `python3 -m py_compile` 与两支脚本 `--report-only` smoke check 均通过，结果仍为 `allow=0/warn=2/block=3` 与 `allow=0/warn=20/block=0`。
- 已完成静态池 4 个 skill 与当前脚本层的正式接线：更新 `~/.codex/skills/static-pool-{expand,promote,cleanup,share}/SKILL.md`，为每个 skill 增补“默认脚本入口”段。当前口径已明确：`expand` 默认优先调用消费品扩池脚本；`promote` 默认优先走通用 `promote_static_pool.py`，历史专项脚本只作 wrapper；`cleanup/share` 暂无稳定统一主脚本，仍以规则驱动为主，只允许借助少量辅助脚本。
- 已同步把 `skill -> script` 映射写回仓库入口文档：更新 `docs/README-静态潜客池.md` 与 `docs/01-机制与规则/README.md`，明确 `expand / promote / cleanup / share` 四条线当前分别处于“专题脚本入口 / 通用脚本入口 / 规则驱动 / 规则驱动”哪种成熟度，避免仓库内外口径继续脱节。
- 已纠正 `static-pool-expand` 的偏置口径：不再把消费品扩池脚本当成全局默认 expand 入口，而是改成“先按主线 / 主画像分流，再判断是否存在匹配的稳定脚本；无匹配脚本则回到规则驱动流程”。同步更新了 `~/.codex/skills/static-pool-expand/SKILL.md`、`docs/README-静态潜客池.md`、`docs/01-机制与规则/README.md`。
- 已在新分支 `codex/execution-layer-foundation` 上开始执行层落地：新增 `shared/static_pool/enrich_engine.py` 与 `scripts/enrich_static_pool.py`，把“补事实 + 重算主画像/次级画像 + 动态回挂强相关知识资产”收成独立 enrich 入口；同时新增 `scripts/expand_static_pool.py` 作为按主线/画像分流的统一 expand 路由入口。
- 已让 `scripts/promote_static_pool.py` 支持消费 enrich 结果包：新增 `--enrich-result-file`，并将 `observation / minimum_fact fail / enrich_not_ready` 直接降为不可进入 `allow` 的 promote 前置约束。
- 已跑通 `Phase 1` 的 `15` 家样本 enrich：生成 `deliveries/phase1_enrich_package_v1.json`，当前摘要为 `result_count=15 / ready_for_promote=11 / observation=4`；并已用 `--write-back` 定向回写这 `15` 家到 `潜客档案库.xlsx / 内部运营-静态潜客池-共享版.xlsx / 治理与证据.xlsx`，第二轮重跑时不再重复创建 queue/evidence。
- enrich 定向回写前已自动备份外部工作簿，当前最新备份为：`潜客档案库.enrich_backup_20260404_192724.xlsx`、`内部运营-静态潜客池-共享版.enrich_backup_20260404_192724.xlsx`、`治理与证据.enrich_backup_20260404_192724.xlsx`。
- 已修正一个关键执行层偏差：对于 `observation` 且纠偏结果明确不给主画像的样本，enrich 不再回退到旧主画像；当前 `acc_popmart / acc_loctek / acc_amec / acc_jereh` 在档案库和共享主表中都已回到“主画像待定、次级画像保留、pending_review”的状态。
- 已基于 enrich 结果重跑 `promote`：生成 `deliveries/phase1_promote_from_enrich_v1.json`，当前摘要为 `allow=7 / warn=4 / block=4`，并确认 `4` 个 observation 样本均未进入 `allow`。
- 已新增并同步入口文档：`docs/00-当前总览/执行层落地分支说明-v1.md`、`docs/00-当前总览/执行层落地分支复盘-v1.md`、`docs/01-机制与规则/enrich执行入口说明-v1.md`、`docs/01-机制与规则/expand-provider路由说明-v1.md`，并更新 `docs/00-当前总览/README.md`、`docs/01-机制与规则/README.md` 挂载这些新入口。
