# Legacy App

本目录存放早期的探迹查询应用壳与部署资产。

包含：

- `apps/`
- `browser_extensions/`
- `collectors/`
- `Dockerfile`
- `docker-compose.yml`
- `docker-compose.prod.yml`
- `alembic.ini`

这些内容已经不属于当前静态潜客池主线。

当前保留它们的原因是：

- 需要保留历史实现上下文
- 研究文档里仍有少量引用
- 后续如需拆分独立仓库，还有迁移依据

如果你正在继续静态潜客池项目，默认不要从这里开始。
