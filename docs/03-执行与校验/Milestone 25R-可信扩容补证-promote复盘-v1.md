# milestone25r_trusted_expansion_v1 promote复盘-v1

## 批次概览

- 目标：`Milestone 25R：可信扩容补证试运行`
- 当前层级：``
- 目标层级：``
- 样本数：`50`
- 判定结果：`allow=0 / warn=50 / block=0`
- 写回结果：`promoted=0 / skipped=50`
- 实际升层：`level_changed=0 / target_level_reached=0`
- 队列与证据：`queue_resolved=0 / evidence_created=0`

## 分对象复盘

### 巴比食品集团有限公司 / `acc_babi`

- 主线 / 画像：`零售消费 / retail_multi_store`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 步步高商业连锁股份有限公司 / `acc_bbg`

- 主线 / 画像：`零售消费 / retail_multi_store`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 广东小熊电器股份有限公司 / `acc_bear`

- 主线 / 画像：`零售消费 / retail_high_sku_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 华熙生物科技股份有限公司 / `acc_bloomage`

- 主线 / 画像：`零售消费 / retail_high_sku_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`knowledge_extraction, verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 光明乳业股份有限公司 / `acc_brightdairy`

- 主线 / 画像：`零售消费 / retail_high_sku_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 中顺洁柔纸业股份有限公司 / `acc_candr`

- 主线 / 画像：`零售消费 / retail_high_sku_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 中国黄金集团黄金珠宝股份有限公司 / `acc_chinagold`

- 主线 / 画像：`零售消费 / retail_multi_store`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 潮宏基珠宝股份有限公司 / `acc_chj`

- 主线 / 画像：`零售消费 / retail_multi_store`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 中粮糖业控股股份有限公司 / `acc_cofco_sugar`

- 主线 / 画像：`零售消费 / retail_high_sku_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 登康口腔护理用品股份有限公司 / `acc_dengkang`

- 主线 / 画像：`零售消费 / retail_high_sku_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 欧亚集团股份有限公司 / `acc_eurasia`

- 主线 / 画像：`零售消费 / retail_multi_store`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 福瑞达生物股份有限公司 / `acc_freda`

- 主线 / 画像：`零售消费 / retail_high_sku_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`promotion_review, verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 深圳市富安娜家居用品股份有限公司 / `acc_fuanna`

- 主线 / 画像：`零售消费 / retail_high_sku_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 盖世食品股份有限公司 / `acc_gaishi`

- 主线 / 画像：`零售消费 / retail_high_sku_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 甘源食品股份有限公司 / `acc_ganyuan`

- 主线 / 画像：`零售消费 / retail_high_sku_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 广州酒家集团股份有限公司 / `acc_guangzhourestaurant`

- 主线 / 画像：`零售消费 / retail_multi_store`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 乖宝宠物食品集团股份有限公司 / `acc_gubei`

- 主线 / 画像：`零售消费 / retail_high_sku_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 天津桂发祥十八街麻花食品股份有限公司 / `acc_guifaxiang`

- 主线 / 画像：`零售消费 / retail_high_sku_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 浙江哈尔斯真空器皿股份有限公司 / `acc_haers`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 浙江海利得新材料股份有限公司 / `acc_hailide`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 海象新材料股份有限公司 / `acc_haixiang`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 海欣食品股份有限公司 / `acc_haixinfood`

- 主线 / 画像：`零售消费 / retail_high_sku_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 好想你健康食品股份有限公司 / `acc_haoxiangni`

- 主线 / 画像：`零售消费 / retail_high_sku_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 江苏恒辉安防股份有限公司 / `acc_hengansecurity`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 恒林家居股份有限公司 / `acc_henglin`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`promotion_review, verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 浙江恒威电池股份有限公司 / `acc_hengwei`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 海融科技股份有限公司 / `acc_hiro`

- 主线 / 画像：`零售消费 / retail_high_sku_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 浙江恒林椅业股份有限公司 / `acc_hlin`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 好莱客创意家居股份有限公司 / `acc_holike`

- 主线 / 画像：`零售消费 / retail_multi_store`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 红旗连锁股份有限公司 / `acc_hqls`

- 主线 / 画像：`零售消费 / retail_multi_store`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 煌上煌集团食品股份有限公司 / `acc_huangshanghuang`

- 主线 / 画像：`零售消费 / retail_high_sku_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 深圳市杰美特科技股份有限公司 / `acc_jame`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`promotion_review, verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 朗科智能电气股份有限公司 / `acc_longood`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 浙江梦天木作家居有限公司 / `acc_mengtian_wood`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 浙江明新旭腾新材料股份有限公司 / `acc_mingxin`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 浙江野马电池股份有限公司 / `acc_mustangbat`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`boundary_review, verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 浙江正特股份有限公司 / `acc_patio`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 深圳市赛维网络科技有限公司 / `acc_sailvan`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`cleanup_review, verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 深圳市三态电子商务股份有限公司 / `acc_santai`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 浙江盛泰服装集团股份有限公司 / `acc_shengtai`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 星徽股份有限公司 / `acc_skshu`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 泰鹏智能家居股份有限公司 / `acc_taipeng`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 广东拓斯达科技股份有限公司 / `acc_topstar`

- 主线 / 画像：`先进制造 / mfg_multi_factory_group`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 永艺家具股份有限公司 / `acc_uechairs`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 浙江华生科技股份有限公司 / `acc_washin`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 星华新材股份有限公司 / `acc_xinghua`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 浙江雅艺金属科技股份有限公司 / `acc_yayi`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 浙江永强集团股份有限公司 / `acc_yotrio`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 深圳市有棵树科技股份有限公司 / `acc_youkeshu`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。

### 浙江正裕工业股份有限公司 / `acc_zhengyu`

- 主线 / 画像：`跨境电商 / cbec_multi_platform_brand`
- 层级变化：`L5 -> L5`，目标=`L3`
- promote 判定：`warn`
- 写回状态：`writeback_applied=False / target_level_reached=False`
- 当前档案状态：`profile_status=active`
- 结论：可继续推进，但仍需显式暴露风险和待补证点。
- 待验证项：M25R 已补最小字段和官方/高可信来源；本轮仅做 report-only，不执行真实写回，不进入正式知识资产。
- 当前仍开放队列：`verification`
- 风险项：
- `persona_boundary_unstable`：当前仍处于观察/边界状态。
- 剩余缺口：
- 当前仍处于观察/边界状态。
