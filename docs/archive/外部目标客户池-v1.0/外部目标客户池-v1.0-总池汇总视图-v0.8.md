# 外部目标客户池 v1.0 总池汇总视图 v0.8

## 1. 文档目的

本文件用于把总池汇总口径正式切换到 `external_target_account_pool_v2` 主表事实源。

从这一版开始：

- 不再沿用早期 `354` 的估算型总池口径
- 以已经进入结构化主表的账户数作为当前最新事实口径
- 后续总池统计优先从结构化主表累计，不再从历史批次文档手工加总

## 2. 当前总量口径

### 2.1 主表事实总量

按 [external_target_account_pool_v2-首版真实内容-v0.12.md](/Users/clairaipartner/.openclaw/workspace-main/bussiness-master/docs/external_target_account_pool_v2-首版真实内容-v0.12.md) 及其之前各版累计，当前结构化静态潜客主表共覆盖 `313` 家目标账户。

这仍然是 `静态潜客匹配` 口径，不引入动态机会判断。

### 2.2 按当前分层统计

- `L1-高可信母样本`：8
- `L2-高可信补充样本`：36
- `L3-中高可信补充样本`：15
- `L4-中可信候选`：14
- `L5-已结构化迁入`：240

### 2.3 当前高质量层

把 `L1 + L2 + L3` 视为当前已经压实的高质量层，则当前高质量层共 `59` 家。

### 2.4 当前结构化覆盖意义

当前 `313` 家不是“旧文档估算总量”，而是已经进入：

- 主线注册表
- 画像注册表
- 知识资产注册表
- 账户主表
- 证据表
- review queue

这一整套结构化体系后的真实覆盖量。

## 3. v0.8 相比 v0.7 的关键变化

### 3.1 汇总口径切到主表事实源

`v0.7` 仍保留了基于早期候选批次文档的 `354` 估算口径。  
本次复核后，所有明确列名的历史候选都已完成结构化迁移，因此后续不再继续沿用该旧估算值。

### 3.2 新增第十三批候选并完成第十轮迁移

新增：

- [外部目标客户池-v1.0-第十三批放量候选池-v0.1.md](/Users/clairaipartner/.openclaw/workspace-main/bussiness-master/docs/外部目标客户池-v1.0-第十三批放量候选池-v0.1.md)
- [external_target_account_pool_v2-首版真实内容-v0.12.md](/Users/clairaipartner/.openclaw/workspace-main/bussiness-master/docs/external_target_account_pool_v2-首版真实内容-v0.12.md)

本轮新增 `20` 家结构化 `L5` 账户，使主表累计覆盖从 `293` 提升到 `313`。

## 4. 当前阶段性判断

到 `v0.8` 为止，静态潜客池已经进入新的阶段：

1. 主体事实源从“批次候选文档”切换到“结构化主表”
2. 总池继续增长，但增长与证据链、队列项同步发生
3. 高质量层保持稳定，结构化 `L5` 持续变厚

## 5. 当前最值得继续做的事

### 5.1 继续扩充结构化主表

后续应继续保持：

- 新增一批高质量 `L5`
- 同步补首条结构化证据
- 同步补 review queue

### 5.2 继续增厚高质量层

当前优先强核验对象：

- 盐津铺子
- 三全食品
- 乐歌股份
- 柳工股份
- 杰瑞股份
- 上海新阳

### 5.3 继续补知识资产

建议优先补：

- 休闲食品 / 速冻食品 / 预制食品品牌消费品画像的知识资产
- 家居与消费电子品牌出海画像的知识资产
- 工程机械与高技术制造画像的知识资产
