# 静态潜客池-L3到L2批量上移专项-v4

## 目标

继续处理上一轮保留的 `49` 家 `L3`，在不降低上移标准的前提下，完成可上移对象的 `L3 -> L2` 批量收口。

## 本轮处理逻辑

对剩余 `49` 家 `L3` 逐一复核后，分成两类：

1. `47` 家仅卡在 `ICP匹配概率 = 中`
   - 已具备：
     - `信息扎实度 = 中高`
     - `profile_status = standard_ready`
     - `official_source_count >= 1`
     - `high_confidence_source_count >= 2`
     - `primary_source_types = 公开主体名单,行业分类,上市公司公开披露`
   - 经本轮 `ICP` 校准后，上移到 `L2`
2. `2` 家仍缺稳定官方入口
   - `追觅创新科技（苏州）有限公司`
   - `深圳市通拓科技有限公司`
   - 本轮只保留在 `L3`，不放松标准硬推上移

## 本轮结果

- 处理前 `L3`：`49`
- 本轮完成上移：`47`
- 本轮保留在 `L3`：`2`

## 最新口径

- `L1 = 8`
- `L2 = 305`
- `L3 = 2`
- `L4 = 1`
- `L5 = 259`
- `L3+ = 315`

## 同步动作

1. 主表中 `47` 家层级更新为 `L2`
2. 档案层：
   - `static_maturity_level = L2`
   - `profile_status = high_quality_ready`
3. 新增 `promotion_assessment` evidence：
   - `ev_{account}_promote_l2_icpcal_20260331`
4. 团队共享索引同步更新：
   - `当前层级`
   - `ICP匹配概率`
   - `档案完整度`
   - `档案页路径`
5. `47` 个客户档案页从 `02-L3档案` 移入 `01-L1-L2档案`
6. 清理了 `2` 个历史残留的错误 `L3` 档案页：
   - `千味央厨食品股份有限公司`
   - `苏州纽威阀门股份有限公司`
   - 这两家公司当前主表层级均为 `L5`，错误页面已移到回收站
7. 重生成 `L3以上客户档案索引.md`

## 当前剩余待处理

当前只剩 `2` 家仍留在 `L3`：

1. `追觅创新科技（苏州）有限公司`
2. `深圳市通拓科技有限公司`

两者后续默认动作一致：
- 优先补官网、官方介绍或一手案例材料
- 在形成稳定官方入口前，不继续上移到 `L2`
